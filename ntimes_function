def print_names(n):
    for i in range(1, n + 1):
        name = input("Enter your name: ")
        print("My name is:", name)

# Get the number of times to prompt the user for their name
n = int(input("Enter number of times: "))

# Call the function to print the names
print_names(n)
